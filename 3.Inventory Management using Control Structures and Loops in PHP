<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Inventory Management</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f7fa;
      margin: 40px;
    }
    h2 {
      text-align: center;
      color: #333;
    }
    table {
      width: 70%;
      margin: 20px auto;
      border-collapse: collapse;
      background: #fff;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    th, td {
      border: 1px solid #ddd;
      padding: 12px;
      text-align: center;
    }
    th {
      background: #007BFF;
      color: #fff;
    }
    tr:nth-child(even) {
      background: #f9f9f9;
    }
    .restock {
      color: red;
      font-weight: bold;
    }
    .ok {
      color: green;
    }
    .summary {
      width: 70%;
      margin: 20px auto;
      text-align: right;
      font-size: 18px;
      font-weight: bold;
      color: #444;
    }
  </style>
</head>
<body>
  <h2>📦 Inventory Management</h2>

  <table>
    <tr>
      <th>Item</th>
      <th>Quantity</th>
      <th>Price (₹)</th>
      <th>Total Value (₹)</th>
      <th>Status</th>
    </tr>
    <?php
    $items = ["Pen" => 20, "Notebook" => 15, "Eraser" => 10];
    $prices = ["Pen" => 5, "Notebook" => 20, "Eraser" => 3];
    $totalInventoryValue = 0;

    foreach ($items as $item => $qty) {
        $price = $prices[$item];
        $value = $qty * $price;
        $totalInventoryValue += $value;

        echo "<tr>";
        echo "<td>$item</td>";
        echo "<td>$qty</td>";
        echo "<td>₹$price</td>";
        echo "<td>₹$value</td>";
        
        if ($qty < 15) {
            echo "<td class='restock'>Restock needed</td>";
        } else {
            echo "<td class='ok'>Sufficient</td>";
        }
        echo "</tr>";
    }
    ?>
  </table>

  <div class="summary">
    <?php echo "Total Inventory Value: ₹$totalInventoryValue"; ?>
  </div>
</body>
</html>
